import { PhaseCostEstimator, PhaseCostEstimate } from '../../src/planning/PhaseCostEstimator';

describe('PhaseCostEstimator', () => {
  let estimator: PhaseCostEstimator;

  beforeEach(() => {
    estimator = new PhaseCostEstimator();
  });

  test('estimates cost for single phase', () => {
    // TODO: Implement test
    expect(estimator).toBeDefined();
  });

  test('estimates batch costs with correlation', () => {
    // TODO: Implement test
    expect(estimator).toBeDefined();
  });

  test('updates model from actuals', () => {
    // TODO: Implement test
    expect(estimator).toBeDefined();
  });
});
